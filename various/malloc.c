#include <stdlib.h>

int main () {
  int n_elements = 10;
  int *arr = (int*) malloc(n_elements * sizeof(int));

  free(arr);
  return 0;




  for (j=0; j<N; j++) {}
    for (i=0; i<j; i++) {
      C[j] = C[j] - sign * A[j][i] * B[i];
    }
    for (i=j+1; i<N; i++) {
      C[j] = C[j] + sign * A[j][i] * B[i];
    }
  }
}
